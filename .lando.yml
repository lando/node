name: lando-node-plugin
services:
  node:
    type: node:18
    build:
      - yarn install
    scanner: false
    ssl: false
    sslExpose: false
tooling:
  node:
    service: node
  yarn:
    service: node
